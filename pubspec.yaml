name: social_iq_live_sdk
description: "Flutter UI SDK for live broadcasts, video/audio calls, and group calls powered by LiveKit."
version: 0.1.0
homepage: https://github.com/yourcompany/social_iq_live_sdk

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  livekit_client: ^2.4.6
  http: ^1.4.0
  permission_handler: ^11.4.0
  socket_io_client: ^3.0.2
  flutter_foreground_task: ^8.17.0
  floating: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
